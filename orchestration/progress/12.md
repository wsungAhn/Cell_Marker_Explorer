# Task 12 — js/export.js (spec 12-export)

- **Verdict:** PASS
- **Fix rounds:** 0
- **Commit:** 5e0631b
- **Tag:** task-12
- **Codex log:** orchestration/logs/12.log
- **Accepted (UTC):** 2026-06-13T15:42Z

## Audit result (vs spec Test Criteria)
- [x] global constructor `ExportController` (ES5 function+prototype; window.ExportController + window.exporter); no real import/export
- [x] API exportCellType/exportCompare/exportOrgan/exportTissueSystem/exportAll/generateCSV/generateTSV/download
- [x] **executed via node against data/cell-markers.json**: generateCSV quotes comma field + escapes `"`→`""`; exportCellType output has **UTF-8 BOM** (0xFEFF) + cell name + `Marker,Type` section; TSV uses tabs
- [x] RFC 4180 escaping; semicolon marker lists; Blob download with filename conventions
- [x] markers via getMarkersForCellType; DOM only inside download()
- [ ] (deferred) actual browser save dialog / Excel BOM open — integration smoke

## Codex report digest
- Assumptions: TSV filename uses `.tsv` ext, same stem as CSV; CSV marker-list columns quoted when non-empty. Flags: none.
- Note: ES5 `function`+`prototype` style (other JS modules use ES6 `class`); functionally equivalent global constructor — meets no-build/global contract.

## Notes for downstream
- Exposes `window.exporter` (instance) — cell-view (09) export button + compare (11) `exportCompare` both resolve via `window.exporter`. Satisfied.
- app.js (16): `window.exporter = new ExportController(datastore)` (constructor already self-assigns window.exporter on first instantiation).
