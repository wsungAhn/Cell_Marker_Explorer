# Task 20 — Microstructure ID Reconcile (spec 24, v1.1.0 Phase 1)

- **Verdict:** PASS (+ supervisor patched validate.py / 01-data-schema for the schema change)
- **Fix rounds:** 0
- **Commit:** 3f1035c
- **Tag:** task-20
- **Codex log:** orchestration/logs/20.log
- **Accepted (UTC):** 2026-06-14T17:xx Z

## Deliverables (Codex)
- `data/cell-markers.json` (svg_region_id removed from all 42 microstructures), `js/organ-view.js` (4 svg_region_id sites removed → canonical `id`)

## Supervisor patches (schema change fallout spec 24 didn't cover)
- `updater/validate.py` — drop `svg_region_id` from microstructure required fields; change the duplicate-region check to use microstructure `id` (was svg_region_id).
- `codex-specs/01-data-schema.md` — remove `svg_region_id` from the microstructure schema + validation rule (canonical `id` is now the SVG region id).

## Audit result
- [x] `node --check js/organ-view.js`; **zero `svg_region_id` in js/**
- [x] data: 12 systems / 42 microstructures / 121 cells; no `svg_region_id` field anywhere
- [x] task-19 `expression_levels` preserved (not clobbered)
- [x] patched `validate.py` reports task-20 data VALID (and old data still VALID — backward compatible)
- [x] regression: datastore loads; `getPathForCellType('hepatocyte')` → microstructureId `hepatic-lobule` (canonical)

## Codex report digest
- FLAG (correct): validate.py still treated svg_region_id as required, so the "passes validate.py" gate was impossible within the data+organ-view-only scope. Resolved by supervisor patching validate.py + 01-data-schema.

## Notes for downstream
- **SVG element ids are NOT yet renamed** (current .svg files still carry old ids like `lobule`). The redraw tasks 21–24 (specs 25–28) MUST emit `<g id="<canonical microstructure id>">` (e.g. `hepatic-lobule`). Until then, microanatomy click-mapping is intentionally in a transitional state (integration smoke after redraws).
- **Milestone m7-data-normalize** reached (marker norm 19 + id reconcile 20).
