# Task 08 — js/organ-view.js (spec 08-organ-view)

- **Verdict:** PASS (logic/structure) — browser visual deferred to integration smoke
- **Fix rounds:** 0
- **Commit:** 5075bf2
- **Tag:** task-08
- **Codex log:** orchestration/logs/08.log
- **Accepted (UTC):** 2026-06-13T15:17Z

## Audit result (vs spec Test Criteria)
- [x] global class `OrganView` (IIFE → window.OrganView; sets window.organView); no import/export
- [x] **render(route) handles BOTH `tissue-system` (organ list) and `organ` (detail)** — router contract
- [x] organ detail: name/description, microanatomy SVG fetch+inject, collapsible `ms-section-<id>`, cell-type items
- [x] markers species-aware via `getMarkersForCellType(id, species)`, capped 3 + "+N more"; `onSpeciesChange` re-render
- [x] SVG region click → highlight + scrollIntoView + expand section; cell-type click → router.navigate to cell detail
- [x] data DOM via createElement/textContent; innerHTML only for trusted microanatomy SVG inject
- [x] `node --check` PASS; structural 10/10
- [ ] (deferred) browser visual/layout/scroll/responsive — integration smoke

## Codex report digest
- Assumptions: `ms-section-<id>` uses dataset `microstructure.id`; SVG region matching accepts BOTH `microstructure.id` and `svg_region_id` (they differ — see below). Flags: none.

## Notes for downstream tasks (CRITICAL DATA FACT)
- **`microstructure.id` ≠ `svg_region_id` in 24 cases** (e.g. `hepatic-lobule`↔`lobule`, `thymic-cortex`↔`cortex`, `vessel-intima`↔`intima`, `nephron-tubules`↔`tubules`, `blood-leukocytes`↔`leukocytes`, `cell-lines-misc`↔`cell-lines`).
  - **Route/URL path segments use `microstructure.id`** (datastore.getPathForCellType returns `microstructureId = microstructure.id`; router breadcrumb uses getMicrostructureById(microstructure.id)).
  - **SVG `<g data-microstructure>` uses `svg_region_id`** (task-03 built region ids from svg_region_id).
  - ⇒ Any module bridging an SVG region click to a route MUST translate svg_region_id → microstructure.id via the datastore. organ-view handles this. **cell-view (09)** and **app.js (16)** must respect: navigation paths = microstructure.id, never svg_region_id.
- cell-view (09) handles route types `microstructure` (cell-type list for that microstructure) and `cell-type` (single cell detail).
