# Task 19 — Marker Normalization (spec 20, v1.1.0 Phase 1)

- **Verdict:** PASS (with 1 minor scope-gap, recorded below)
- **Fix rounds:** 0
- **Commit:** 31030b2
- **Tag:** task-19
- **Codex log:** orchestration/logs/19.log
- **Accepted (UTC):** 2026-06-14T17:11Z

## Deliverables (5 files, existing-file modifications swapped into live tree)
- `updater/normalize_markers.py` (new), `data/cell-markers.json` (normalized), `js/links.js`, `css/styles.css`, `js/cell-view.js`

## Audit result (vs spec 20 Test Criteria) — 11/11 + validate + regression
- [x] py_compile (normalize_markers.py) + node --check (links.js, cell-view.js)
- [x] structure intact: 12 tissue systems, 121 cell types preserved
- [x] no `+`/`-`/`++`/`low`/`high`/`dim` suffix markers anywhere
- [x] no spelled-out `alpha`/`beta`
- [x] `TGFB1` replaces `TGF-beta`; `ACTA2` replaces `Alpha-SMA/ACTA2`
- [x] `expression_levels` added where qualifiers existed; no duplicate markers within arrays
- [x] links.js MARKER_ALIASES has old→new (TGF-beta→TGFB1 etc.)
- [x] cell-view.js reads `expression_levels` → `dataset.expressionLevel`; css badge `[data-expression-level]`
- [x] validate.py reports normalized dataset VALID (0 errors)
- [x] regression: datastore loads normalized data; hepatocyte/CD68(9)/FOXP3 queries work

## Codex report digest
- Assumptions: applied spec's exact map first; generically normalized additional in-data low/high/dim suffixes (Test Criteria forbid them); left `hi`/`bright` shorthand unchanged (not in spec list); script defaults output to a `.normalized.json` to avoid clobbering live data, wrote outbox copy via `--output`.

## SCOPE GAP (minor, recorded — supervisor prompt omission, not a Codex defect)
- spec 20 also asks to update the **datastore search index** so a search for the OLD name (`TGF-beta`) still returns M2c/M2d macrophages via alias. `js/datastore.js` was NOT in this task's 5-file scope (my prompt omitted it), so old-name search is not yet wired. links.js aliases (used for external-link gene resolution) are in place. 
- **Follow-up:** add old→new marker aliases to datastore's search index — fold into task 26 (citations, which already modifies datastore.js) or a small dedicated patch. Impact: low (users searching the canonical names work fine today).

## Notes for downstream
- New data field `markers.<species>.expression_levels{}` — compare (task 11) / export (task 12) may optionally surface levels later; not required for v1.1.0.
