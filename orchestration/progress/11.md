# Task 11 — js/compare.js (spec 11-compare)

- **Verdict:** PASS
- **Fix rounds:** 0
- **Commit:** 0d24df2
- **Tag:** task-11
- **Codex log:** orchestration/logs/11.log
- **Accepted (UTC):** 2026-06-13T15:36Z

## Audit result (vs spec Test Criteria)
- [x] global class `CompareController`; no real import/export
- [x] API addCellType/removeCellType/clearAll/getSelectedIds/isInCompare/renderTray/showTray/hideTray/renderCompareView/onSelectionChange
- [x] `render(route)` reads route.params.cellTypeIds (router contract); exposes compareTray/compareView for cell-view + router
- [x] union-marker table, +/−/· statuses, sort shared/unique/alpha, show-negative toggle, species selector, .row-shared highlight
- [x] markers via getMarkersForCellType; all DOM via createElement (no innerHTML)
- [x] **executed 6/6 via node against data/cell-markers.json**: add→getSelectedIds, isInCompare, no-duplicate, **max 6 enforced** (warns), removeCellType, clearAll
- [ ] (deferred) tray UI / table visual / sort interaction — integration smoke

## Codex report digest
- Assumptions: export button disabled until `window.exporter`/`window["export"].exportCompare` exists (task 12); route-rendered compare ids become the in-memory tray selection (tray ↔ compare view synced). Flags: none.

## Notes for downstream
- cell-view (09) add-to-compare wires to `window.compareTray`/`compare` `.addCellType`/`.isInCompare` — satisfied.
- export (12) must expose `window.exporter`/`window["export"]` with `exportCompare` (+ single-cell export for cell-view). app.js (16) instantiates CompareController and assigns globals (compareController/compareTray/compareView).
