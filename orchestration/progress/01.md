# Task 01 — js/datastore.js (spec 05-datastore)

- **Verdict:** PASS
- **Fix rounds:** 0
- **Commit:** 07f5638
- **Tag:** task-01
- **Codex log:** orchestration/logs/01.log
- **Accepted (UTC):** 2026-06-13T14:35Z

## Audit result (vs spec Test Criteria)
- [x] `load()` resolves with valid JSON (file-backed fetch shim)
- [x] `getTissueSystems()` returns 12
- [x] `getCellTypeById('hepatocyte')` → Hepatocyte
- [x] `findCellTypesByMarker('CD68','human')` includes kupffer-cell (+ other macrophages)
- [x] `search('FOXP3')` returns treg-cell
- [x] `search('liver')` returns liver organ + hepatocyte + kupffer-cell
- [x] `setSpecies('mouse')` fires registered callback
- [x] `getPathForCellType('hepatocyte')` → {digestive, liver, hepatic-lobule, hepatocyte}
- [x] extra: invalid species throws; empty query → []
- [x] data-bound checks executed: 10/10 PASS via node against data/cell-markers.json

## Codex report digest
- Assumptions: search returns marker matches as `cell_type` results (matchField 'marker') for direct navigation; cell-type search index also includes ancestor organ/microstructure/tissue names so organ queries (e.g. "liver") surface resident cells.
- Flags: task text example said microstructureId `'lobule'` for hepatocyte; dataset uses `'hepatic-lobule'`. Implementation followed the dataset (correct — dataset is source of truth).

## Needs human glance (if any)
- None.

## Notes for downstream tasks
- Global class is `window.CellMarkersDatastore` (no import/export); `js/app.js` will `new CellMarkersDatastore()`.
- Search result `path` format: `#/<tissueSystem>/<organ>/<microstructure>/<cellType>` — router (task 06) must match this fragment shape.
- `onDataLoaded` / `onSpeciesChange` are the subscribe hooks views depend on.
