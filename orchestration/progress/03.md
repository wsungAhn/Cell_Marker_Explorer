# Task 03 — svg/microanatomy/*.svg (27 files) (spec 03-microanatomy-svgs)

- **Verdict:** PASS (structure) — visual quality flagged for human glance
- **Fix rounds:** 0
- **Commit:** cf706ae
- **Tag:** task-03
- **Codex log:** orchestration/logs/03.log
- **Accepted (UTC):** 2026-06-13T14:53Z

## Audit result (vs spec Test Criteria)
- [x] all 27 organ SVGs exist
- [x] XML well-formed (xmllint, all 27)
- [x] **per-organ `<g>` id set == dataset `svg_region_id` exactly** (no extra/missing) — 27/27
- [x] viewBox="0 0 800 600" on all
- [x] each clickable group: data-microstructure/role/tabindex/aria-label + child title/desc
- [x] every region has a matching `data-for` label
- [x] no `<script>`, no external/http href
- [x] data-bound checks executed: 28/28 + 3/3 via node against data/cell-markers.json
- [ ] (deferred) visual render at 400/800px, label overlap, click nav (needs body-map/organ-view + css)

## Codex report digest
- Assumptions: sibling shading via `fill-opacity` on the parent tissue-system hex (avoids off-palette colors); single-microstructure organs use the organ shape as the one clickable region with a non-interactive outline behind it.
- Flags: none. Visual smoke could not run in Codex env (iab/qlmanage/no SVG render lib).

## Needs human glance
- **Visual rendering quality** at 400 / 800 px (histology plausibility, label overlap, region legibility) across all 27. Defer to integration smoke.

## Notes for downstream tasks
- organ-view.js (task 08) loads `svg/microanatomy/<organ>.svg` and binds clicks on `g.microstructure-region` via `data-microstructure` (= dataset `svg_region_id`).
- Organ→file mapping is dataset `organs[].microanatomy_svg` (basename). Region fills use tissue-system colors + fill-opacity for siblings → css/styles.css (task 05) hover/focus vars `--hover-glow`/`--accent-blue`.
