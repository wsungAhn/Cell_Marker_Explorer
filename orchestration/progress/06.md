# Task 06 — js/router.js (spec 06-router)

- **Verdict:** PASS
- **Fix rounds:** 0
- **Commit:** fe9af2f
- **Tag:** task-06
- **Codex log:** orchestration/logs/06.log
- **Accepted (UTC):** 2026-06-13T15:06Z

## Audit result (vs spec Test Criteria)
- [x] global class `Router` (no import/export; window.Router); constructor(datastore)
- [x] resolveRoute 7 types correct: body-map / tissue-system / organ / microstructure / cell-type / search / compare
- [x] `#/digestive/liver/hepatic-lobule/hepatocyte` → cell-type w/ all 4 params
- [x] `#/search/FOXP3` → search; `#/compare/hepatocyte,kupffer-cell` → cellTypeIds array
- [x] validateRoute: invalid tissue→body-map, invalid organ→tissue parent, compare prunes invalid ids
- [x] breadcrumb rebuilt via createElement (no innerHTML); back/forward wired; debounce 100ms; keyboard Backspace/Escape/Alt+Arrows
- [x] data-bound checks executed: 11/11 via node against data/cell-markers.json
- [ ] (deferred) browser back/forward + view render wiring — integration smoke with views

## Codex report digest
- Assumptions: view renderers exposed either as `render()` on the target `.view` element OR as global instances (`bodyMapView`/`bodyMap`, `organView`, `cellView`, `searchView`, `compareView`) — spec 06 defines no renderer registry. Flags: none.

## Notes for downstream tasks (IMPORTANT — view contract)
- **route→view container map** (router owns this): body-map→`view-body-map`; tissue-system AND organ→`view-organ`; microstructure AND cell-type→`view-cell`; search→`view-search`; compare→`view-compare`.
  - ⇒ organ-view (task 08) must handle BOTH `tissue-system` (organ list) and `organ` (microanatomy detail) route types.
  - ⇒ cell-view (task 09) must handle BOTH `microstructure` (cell-type list) and `cell-type` (cell detail) route types.
- Each view module (07/08/09/10/11) should expose a global instance named `bodyMapView`/`organView`/`cellView`/`searchView`/`compareView` (or attach `render(route)` to its `.view` element) and implement `render(route)`.
- app.js (16) wires: `new Router(datastore)`, `.start()`, and the global view instances.
