# Task 04 — index.html (spec 04-app-shell)

- **Verdict:** PASS
- **Fix rounds:** 0
- **Commit:** e2bacb8
- **Tag:** task-04
- **Codex log:** orchestration/logs/04.log
- **Accepted (UTC):** 2026-06-13T14:56Z

## Audit result (vs spec Test Criteria)
- [x] HTML5 doc, `lang="en"`, `data-theme="light"`
- [x] css link + favicon link; OG/Twitter meta
- [x] skip-link → #main-content; landmark roles + aria
- [x] all 22 required structural ids present
- [x] five view containers; view-body-map is `.view active`, others hidden
- [x] species toggle: human(active, aria-checked=true) + mouse
- [x] **12 deferred scripts in exact order, app.js last**
- [x] noscript fallback with Labome link
- [x] data-bound checks executed: 8/8 via node
- [ ] (deferred) validator.w3.org, responsive 320/768/1200, keyboard shortcuts, dark-mode/species runtime — need CSS (05) + JS

## Codex report digest
- Assumptions: none. Flags: none.

## Needs human glance / notes for downstream
- **favicon missing:** `svg/icons/favicon.svg` is referenced but does not exist (no task in the 18-task plan generates it). Harmless 404. Decide at integration smoke: add a small favicon.svg or drop the `<link rel="icon">`.
- View container ids (`view-body-map/organ/cell/search/compare`), `.view`/`.active` switching, breadcrumb `#/` fragment, and all the header/compare ids are the contract the router (06), views (07–09), search (10), compare (11), species-toggle (13), update-badge (15), and app.js (16) must bind to.
- CSS (task 05) must define `.skip-link`, `.view`/`.active` (0.2s fade), `.compare-tray`, `.breadcrumb`, theme via `data-theme`, and the `--hover-glow`/`--accent-blue` vars the SVGs use.
