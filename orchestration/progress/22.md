# Task 22 — Citations Table CODE infrastructure (spec 21, v1.1.0)

- **Verdict:** PASS (code infra; citation DATA deferred to scraper track)
- **Fix rounds:** 0
- **Commit:** 60bd401
- **Tag:** task-22
- **Codex log:** orchestration/logs/22.log
- **Accepted (UTC):** 2026-06-14T17:xx Z

## Deliverables
- `data/cell-markers.json` (`metadata.citations = []` placeholder), `js/datastore.js` (getCitation/getCitationsForCellType/citationById + old-name search alias), `js/cell-view.js` (structured refs + "Citation details pending" fallback + "No references available"), `css/styles.css` (references styles)

## Audit result — 10/10 + validate
- [x] node --check datastore + cell-view; `metadata.citations === []`
- [x] datastore getCitation/getCitationsForCellType + citationById (guards empty)
- [x] cell-view reference-unresolved fallback; css references-list styles
- [x] regression: 12 systems, CD68/FOXP3 still work; getCitation(1)→null, getCitationsForCellType(hepatocyte)→[] (no crash)
- [x] **old-name search alias resolved (task-19 gap closed): `TGF-beta` search returns the TGFB1 cell types** (datastore carries old→new alias map, no link.js load-order dependency)
- [x] data passes validate.py

## Codex report digest
- Assumptions: citation contents deferred per scope; unresolved refs show "Citation details pending" until metadata.citations populated; datastore carries its own old→new alias map (no runtime dependency on links.js load order). Flags: none.

## DEFERRED — citation DATA (scraper track, needs network)
- `metadata.citations[]` is empty. Populating it requires scraping the 4 Labome reference sections (156 citations, ID-conflict handling per spec 21 §4) — a network operation, same track as task-17's live scrape. Until then all references render the "Citation details pending" fallback (graceful). 
- Closes task-09's original data gap at the CODE level; DATA still pending the live scrape on the main system.
