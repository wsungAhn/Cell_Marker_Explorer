# Task 09 — js/cell-view.js (spec 09-cell-view)

- **Verdict:** PASS (logic/structure) — browser visual deferred; one DATA GAP flagged for final report
- **Fix rounds:** 0
- **Commit:** 8af7a88
- **Tag:** task-09
- **Codex log:** orchestration/logs/09.log
- **Accepted (UTC):** 2026-06-13T15:24Z

## Audit result (vs spec Test Criteria)
- [x] global class `CellView` (window.CellView; sets window.cellView); no real import/export ("Export CSV"/window['export'] are word-matches, not ES modules)
- [x] **render(route) handles BOTH `microstructure` (cell list) and `cell-type` (detail)** — router contract
- [x] positive `.marker-tag.positive` + negative `.marker-tag.negative`; both species (side-by-side/tabbed); active = getSpecies()
- [x] marker tag click → `router.navigate('#/search/' + encodeURIComponent(marker))`
- [x] compare-add + export buttons (graceful: disabled if global module absent); external links via links module or fallback URL templates
- [x] references mapped to metadata.sources; provenance (source/added_in/last_modified)
- [x] species-aware re-render via onSpeciesChange; all DOM via createElement/textContent (no innerHTML)
- [x] navigation paths use `microstructure.id` (not svg_region_id); `node --check` PASS; structural 10/10
- [ ] (deferred) browser visual/responsive/tabbed — integration smoke

## Codex report digest
- Assumptions: CSS (15) provides `.species-marker-panels`/`.species-tab`/`.active` desktop-vs-mobile presentation; compare/export disabled when global modules absent.
- FLAG: see DATA GAP below.

## DATA GAP — escalate in FINAL-REPORT (not a code defect, not loop-blocking)
- `cellType.references` are numeric citation IDs **1–157** (156 distinct), but `data/cell-markers.json` has **no reference/citation table** and only **6** `metadata.sources`. Full reference→source mapping is impossible with current data.
- cell-view maps a reference to a source only when a valid 1-based index into `metadata.sources` exists (refs 1–6); otherwise shows `Reference N` without inventing metadata (correct per No-Guessing/Explainability).
- **Recommendation for user (Phase 1 data):** add a citations table to the dataset (id→{title,doi,url}) or scope references to the 6 sources. Affects every cell-type detail view's References section.

## Notes for downstream
- Marker tag click emits `#/search/<marker>` → search view (task 10) must resolve a bare marker query.
- compare-add-btn expects `window.compareTray`/`compare` (task 11); export button expects `window.exporter`/`export` (task 12); links via `window.links` (task 14). app.js (16) wires these globals.
