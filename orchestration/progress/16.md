# Task 16 — js/app.js (spec 18-app-init)

- **Verdict:** PASS (structure/wiring) — browser boot verified at integration smoke
- **Fix rounds:** 0
- **Commit:** f9984f4
- **Tag:** task-16
- **Codex log:** orchestration/logs/16.log
- **Accepted (UTC):** 2026-06-13T16:06Z

## Audit result (vs spec Test Criteria)
- [x] no import/export (last defer script); AppBus EventTarget + emit/on; 5 canonical events
- [x] instantiates all 11 modules in boot order; `datastore.load()` (Promise .then/.catch, equivalent to await) with error panel + Retry on failure
- [x] speciesToggle.init/search.init/updateBadge.init; router.onRouteChange(dispatchView) + router.start()
- [x] dispatchView covers all route types; assigns globals router resolves; version badge from datastore (not hard-coded)
- [x] keyboard shortcuts (/ Esc Backspace C H) guarded against text inputs; window.App debug namespace; no innerHTML for data
- [x] structural 13/13 via node (+ node --check)
- [ ] browser boot / deep link / end-to-end — see integration smoke (this commit)

## Codex report digest
- Assumptions: Retry only cleans app/router listeners (data-load failure stops before controllers bind DOM); search dispatch uses SearchController.render() with renderResults fallback.
- FLAG (resolved correctly): spec 18 dispatch table says microstructure→OrganView, but the assembled runtime (router.js maps microstructure→#view-cell; CellView handles microstructure/cell-type; OrganView handles tissue-system/organ) is what app.js follows. Consistent with the contract established in tasks 06/08/09. No defect.

## Integration smoke (browser, static server localhost) — PASS
Ran the assembled app (index.html + 12 JS + css + 28 SVG + data) in a real browser:
- [x] Boots to body-map with **0 console errors/warnings**; `window.App` exposes all 11 instances + bus/state
- [x] body-map SVG injected, **12 labelled regions** rendered; version badge `v1.0.0`, footer `Data v1.0.0 — Updated 2024-06-27`
- [x] Deep link `#/digestive/liver/hepatic-lobule/hepatocyte` → cell-type view shows Hepatocyte; breadcrumb full path
- [x] `#/digestive` → tissue-system (view-organ); `#/digestive/liver` → organ detail with microanatomy SVG
- [x] species toggle → datastore 'mouse'; `#/search/CD68` → search results; `#/compare/hepatocyte,kupffer-cell` → compare table (9 rows)
- Screenshot captured during smoke (header + body map + compare tray all correct).

## Notes
- **Milestone m5-features** reached (update-badge 15 + app.js 16 wired). **App is functionally complete & verified end-to-end** at v1.0.0 runtime. Remaining tasks 17–18 are the Python updater pipeline (independent of app runtime).
