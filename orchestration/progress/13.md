# Task 13 — js/species-toggle.js (spec 13-species-toggle)

- **Verdict:** PASS
- **Fix rounds:** 0
- **Commit:** 3c8c9ae
- **Tag:** task-13
- **Codex log:** orchestration/logs/13.log
- **Accepted (UTC):** 2026-06-13T15:48Z

## Audit result (vs spec Test Criteria)
- [x] global class `SpeciesToggle` (window-exposed); no import/export
- [x] init/getSpecies/setSpecies/onSpeciesChange; binds `#species-toggle .species-btn`
- [x] setSpecies → `datastore.setSpecies` (datastore = source of truth); active class + aria-checked update
- [x] localStorage persist (key `cell-markers-species`), default human, try/catch safe
- [x] keyboard Left/Right select; debounce 100ms (immediate persist/aria, debounced re-render callbacks)
- [x] **executed 10/10 via node**: default human, setSpecies(mouse)→datastore mouse, persisted to localStorage, init reads stored mouse
- [ ] (deferred) live button visual / cross-view re-render — integration smoke

## Codex report digest
- Assumptions: localStorage key `cell-markers-species`; datastore.setSpecies called immediately, onSpeciesChange callbacks debounced 100ms. Flags: none.

## Notes for downstream
- Views (organ 08 / cell 09 / compare 11 / search 10) re-render via `datastore.onSpeciesChange` — datastore is the single species source of truth; this toggle just drives it + persists.
- app.js (16): `window.speciesToggle = new SpeciesToggle(datastore)` then `.init()`.
